for lookup table names, the table must end with "code"
and contain a name and description column

many to many association tables have a _has_

all table names, columns are in lowercase

all tables have a createdtime and modifiedtime
